1. Create a Welcome component that accepts a name prop and displays: “Hello, [name]!”
2. Build a ProfileCard component to display user details like name, age, and location using props.
3. Create a component that takes a list of hobbies as props and displays them as a list.
4. Pass a default prop value for a component that displays a user’s favourite color.
5. Create a parent component that passes data (e.g., a movie list) to a child component via props.
